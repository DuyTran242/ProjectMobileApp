<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- 1. Toolbar giống như “Thay đổi mật khẩu” -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/toolbar_bg"
        android:paddingTop="@dimen/status_bar_height">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar_change_password"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/toolbar_bg"
            android:elevation="4dp"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:navigationIconTint="@color/toolbar_nav_icon"
            app:title="Thay đổi mật khẩu"
            app:titleTextColor="@color/toolbar_title_text"
            app:popupTheme="@style/ThemeOverlay.MaterialComponents.Light" />
    </com.google.android.material.appbar.AppBarLayout>

    <!-- 2. Ô nhập "Mật khẩu mới" ngay dưới Toolbar -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/til_new_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:hint="Mật khẩu mới"
        app:boxBackgroundMode="outline"
        app:boxStrokeColor="@color/divider_gray"
        app:endIconMode="password_toggle"
        app:endIconTint="@color/text_secondary">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/et_new_password"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textPassword"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"
            android:textColor="@color/text_primary"
            android:textSize="16sp" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- 3. Ô nhập "Xác nhận mật khẩu mới" nằm ngay dưới "Mật khẩu mới" -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/til_confirm_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:hint="Xác nhận mật khẩu mới"
        app:boxBackgroundMode="outline"
        app:boxStrokeColor="@color/divider_gray"
        app:endIconMode="password_toggle"
        app:endIconTint="@color/text_secondary">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/et_confirm_password"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textPassword"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"
            android:textColor="@color/text_primary"
            android:textSize="16sp" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- 4. Nút "Xác nhận" nằm bên dưới trường xác nhận mật khẩu mới -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_submit_change_password"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:text="Xác nhận"
        style="@style/Widget.MaterialComponents.Button"
        app:cornerRadius="4dp"
        app:backgroundTint="@color/colorPrimary"
        android:textColor="@android:color/white" />

    <!-- 5. (Nếu cần) Nội dung bổ sung khác có thể đặt ở đây -->
</LinearLayout>
